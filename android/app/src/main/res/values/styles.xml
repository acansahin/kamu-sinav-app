<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
        Açılış ekranı. `androidx.core:core-splashscreen` bu üç niteliği API
        24'e kadar kendi compat drawable'ıyla geriye taşır, yoğunluk başına
        splash.png gerekmez.

        BridgeActivity `installSplashScreen()` çağırmaz; onCreate içinde
        doğrudan `setTheme(AppTheme_NoActionBar)` yapar. `postSplashScreenTheme`
        yine de bildirilir — geçişi kütüphane yönetirse doğru temaya düşsün.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>